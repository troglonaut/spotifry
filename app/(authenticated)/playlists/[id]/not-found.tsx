import Typography from "@mui/material/Typography";

export default function PlaylistNotFound() {
  return (
    <main>
      <Typography variant="h2">{`No playlist like that here, champ.`}</Typography>
      <Typography variant="h3">VERY SAD.</Typography>
    </main>
  );
}
