import Typography from "@mui/material/Typography";

export default function ArtistNotFound() {
  return (
    <main>
      <Typography variant="h1">{`Can't find that ARTIST, pal.`}</Typography>
      <Typography variant="h2">Derrrrp.</Typography>
    </main>
  );
}
