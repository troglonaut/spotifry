import Typography from "@mui/material/Typography";

export default function UserNotFound() {
  return (
    <main>
      <Typography variant="h1">{`Can't find that user, buddy.`}</Typography>
      <Typography variant="h2">whooooooooooooops.</Typography>
    </main>
  );
}
